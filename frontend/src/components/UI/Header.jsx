import React from 'react';
import AppBar from '@material-ui/core/AppBar';
import Toolbar from '@material-ui/core/Toolbar';

const Header = () => (
    <AppBar>
        <Toolbar>canny</Toolbar>{' '}
    </AppBar>
);
export default Header;
